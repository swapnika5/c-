#include<iostream.h>
#include<conio.h>
void main()
{
int num,count=0;
clrscr();
cout<<"Enter a number";
cin>>num;
while(num>0)
{
num&=(num-1);
count++;
}
cout<<"the number of 1 bits is : "<<count;
getch();
}
